{% extends 'base.html.twig' %}

{% block title %}Shipping index{% endblock %}

{% block body %}
    <h1>Shipping index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>AddressLine1</th>
                <th>AddressLine2</th>
                <th>City</th>
                <th>Zipcode</th>
                <th>Country</th>
                <th>Phone</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for shipping in shippings %}
            <tr>
                <td>{{ shipping.id }}</td>
                <td>{{ shipping.addressLine1 }}</td>
                <td>{{ shipping.addressLine2 }}</td>
                <td>{{ shipping.city }}</td>
                <td>{{ shipping.zipcode }}</td>
                <td>{{ shipping.country }}</td>
                <td>{{ shipping.phone }}</td>
                <td>
                    <a href="{{ path('shipping_show', {'id': shipping.id}) }}">show</a>
                    <a href="{{ path('shipping_edit', {'id': shipping.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('shipping_new') }}">Create new</a>
{% endblock %}
