{% extends 'base.html.twig' %}

{% block title %}Billing{% endblock %}

{% block body %}
    <h1>Billing</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ billing.id }}</td>
            </tr>
            <tr>
                <th>AddressLine1</th>
                <td>{{ billing.addressLine1 }}</td>
            </tr>
            <tr>
                <th>AddressLine2</th>
                <td>{{ billing.addressLine2 }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ billing.city }}</td>
            </tr>
            <tr>
                <th>Zipcode</th>
                <td>{{ billing.zipcode }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ billing.country }}</td>
            </tr>
            <tr>
                <th>Phone</th>
                <td>{{ billing.phone }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('billing_index') }}">back to list</a>

    <a href="{{ path('billing_edit', {'id': billing.id}) }}">edit</a>

    {{ include('billing/_delete_form.html.twig') }}
{% endblock %}
